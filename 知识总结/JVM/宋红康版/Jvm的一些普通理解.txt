因为不同平台有着不同的cpu，而不同的cup对应着不同的指令集解读，
所以要高级语言跨平台，就要屏蔽掉cpu之间的差异性，如何屏蔽，
可以联想到适配器模式，通过上转接口来进行运行，
比如Java就通过直接运行虚拟机，每个每个虚拟机都针对于不同的指令集，
这样Java编写的程序满足于Java的规则即可，不同的虚拟机都是符合Java的标准的，
这样就只需要通过jvm来与操作系统进行交互，调用资源，
也能理解为什么编译原理中将jvm也称作一个编译器。